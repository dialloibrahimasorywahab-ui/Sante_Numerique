# Shared

Point d'entrée des composants et utilitaires réutilisables dans plusieurs features.

Les composants globaux existants sont réexportés depuis `shared/components`. Les nouveaux éléments partagés doivent être ajoutés ici plutôt que dans une feature métier.
